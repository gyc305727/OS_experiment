# 实验4：多道程序与协作式调度 - 过程记录

> 本文件是实验4的连续执行命令稿和阶段记录稿，不是最终实验报告。  
> 命令按“Windows PowerShell -> openEuler 容器 Bash -> /mnt 仓库根目录”的顺序组织。  
> 正式实验时应从阶段0开始顺序执行，每个阶段完成后截图整个命令行窗口，并把真实输出补入对应记录位置。

## 阶段0：启动并进入实验容器

目的：从 Windows PowerShell 进入已配置好的 openEuler 实验容器，确认后续命令统一在容器内 `/mnt` 仓库根目录执行。

命令：

```powershell
Set-Location -LiteralPath "D:\GYC\2026.3-2026.7\操作系统\experiment"
docker start gardener-exp1
docker exec -it gardener-exp1 bash
```

进入容器后继续执行：

```bash
cd /mnt
pwd
ls
rustc --version
cargo --version
qemu-system-riscv64 --version
```

成功标志：

- PowerShell 能启动 `gardener-exp1` 并进入容器 Bash。
- 容器内 `pwd` 输出 `/mnt`。
- `ls` 能看到 `os`、`user`、`bootloader`、`rust-toolchain` 等实验仓库内容。
- Rust、Cargo、QEMU 能输出版本信息。

截图占位：图4-1 实验4环境与容器入口确认。

REPORT_SYNC_BLOCK：

- 本阶段目标：确认实验4从 Windows 仓库进入 openEuler 容器，并统一切换到 `/mnt`。
- 实际执行命令：见本阶段命令块。
- 成功标志：见本阶段成功标志。
- 需要截图的位置：阶段0全部命令执行完成后，截图整个命令行窗口。
- 本阶段修改的文件：无。
- 报告素材草稿：本阶段确认实验容器能够访问挂载到 `/mnt` 的实验仓库，Rust、Cargo 和 QEMU 工具链可用于后续构建与运行。
- 问题与处理：待填写。
- 是否建议进入下一 checkpoint：环境确认通过后进入阶段1。

## 阶段1：确认实验3代码基线

目的：确认实验4是在实验3批处理与特权级代码基础上继续扩展，检查 `os` 和 `user` 的关键文件。

命令：

```bash
cd /mnt
ls docs
ls reports
ls records
ls user/src
ls user/src/bin
ls os/src
ls os/src/trap
ls os/src/syscall
sed -n '1,160p' user/src/syscall.rs
sed -n '1,180p' user/src/lib.rs
sed -n '1,220p' os/src/batch.rs
sed -n '1,160p' os/src/trap/context.rs
sed -n '1,140p' os/src/syscall/mod.rs
sed -n '1,120p' os/src/syscall/process.rs
sed -n '1,120p' os/src/main.rs
```

成功标志：

- 能看到 `user` 项目中已有 `src/bin`、`console.rs`、`syscall.rs`、`lib.rs`。
- 能看到 `os` 项目中已有 `batch.rs`、`trap`、`syscall`、`link_app.S`。
- `batch.rs` 当前仍是实验3单应用顺序加载风格，后续会拆分为 loader/task 结构。

截图占位：图4-2 实验3代码基线确认。

REPORT_SYNC_BLOCK：

- 本阶段目标：确认实验4开始前的代码基线。
- 实际执行命令：见本阶段命令块。
- 成功标志：见本阶段成功标志。
- 需要截图的位置：显示关键目录和关键文件内容后截图整个命令行窗口。
- 本阶段修改的文件：无。
- 报告素材草稿：本阶段确认实验4是在已有用户库、系统调用、Trap 处理和批处理应用加载代码基础上继续实现。
- 问题与处理：待填写。
- 是否建议进入下一 checkpoint：基线确认后进入阶段2。

## 阶段2：调整用户程序构建方式

目的：为每个用户程序设置不同加载地址，并通过 `build.py` 配合 `Makefile` 生成多个用户程序二进制。

命令：

```bash
cd /mnt/user
cat > build.py <<'EOF'
import os

base_address = 0x80400000
step = 0x20000
linker = 'src/linker.ld'

app_id = 0
apps = os.listdir('src/bin')
apps.sort()
for app in apps:
    app = app[:app.find('.')]
    lines = []
    lines_before = []
    with open(linker, 'r') as f:
        for line in f.readlines():
            lines_before.append(line)
            line = line.replace(hex(base_address), hex(base_address + step * app_id))
            lines.append(line)
    with open(linker, 'w+') as f:
        f.writelines(lines)
    os.system('cargo build --bin %s --release' % app)
    print('[build.py] application %s start with address %s' % (app, hex(base_address + step * app_id)))
    with open(linker, 'w+') as f:
        f.writelines(lines_before)
    app_id = app_id + 1
EOF

cat > Makefile <<'EOF'
.RECIPEPREFIX := >
TARGET := riscv64gc-unknown-none-elf
MODE := release
APP_DIR := src/bin
TARGET_DIR := target/$(TARGET)/$(MODE)
APPS := $(wildcard $(APP_DIR)/*.rs)
ELFS := $(patsubst $(APP_DIR)/%.rs, $(TARGET_DIR)/%, $(APPS))
BINS := $(patsubst $(APP_DIR)/%.rs, $(TARGET_DIR)/%.bin, $(APPS))

OBJDUMP := rust-objdump --arch-name=riscv64
OBJCOPY := rust-objcopy --binary-architecture=riscv64

elf: $(APPS)
>python3 build.py

binary: elf
>$(foreach elf, $(ELFS), $(OBJCOPY) $(elf) --strip-all -O binary $(patsubst $(TARGET_DIR)/%, $(TARGET_DIR)/%.bin, $(elf));)

build: binary

clean:
>cargo clean

.PHONY: elf binary build clean
EOF

sed -n '1,120p' build.py
sed -n '1,120p' Makefile
make build
ls -lh target/riscv64gc-unknown-none-elf/release
```

成功标志：

- `build.py` 输出每个应用对应的起始地址。
- `make build` 能生成 `.bin` 文件。
- 输出中能看到用户程序按 `0x80400000`、`0x80420000` 等间隔链接。

截图占位：图4-3 用户程序按不同地址构建。

REPORT_SYNC_BLOCK：

- 本阶段目标：让用户程序支持多道程序场景下的不同加载地址。
- 实际执行命令：见本阶段命令块。
- 成功标志：`make build` 成功，并生成多个用户程序二进制。
- 需要截图的位置：显示 `build.py` 输出、`make build` 结果和产物列表后截图整个命令行窗口。
- 本阶段修改的文件：`user/build.py`、`user/Makefile`。
- 报告素材草稿：本阶段通过构建脚本为每个用户程序临时改写链接地址，使不同应用在内存中具有不同起始地址。
- 问题与处理：待填写。
- 是否建议进入下一 checkpoint：用户程序构建通过后进入阶段3。

## 阶段3：增加 yield 系统调用和测试应用

目的：在用户态封装 `yield_()`，并创建三个主动让出 CPU 的测试应用。

命令：

```bash
cd /mnt/user
cat > src/syscall.rs <<'EOF'
use core::arch::asm;

const SYSCALL_WRITE: usize = 64;
const SYSCALL_EXIT: usize = 93;
const SYSCALL_YIELD: usize = 124;

fn syscall(id: usize, args: [usize; 3]) -> isize {
    let mut ret: isize;
    unsafe {
        asm!(
            "ecall",
            in("x10") args[0],
            in("x11") args[1],
            in("x12") args[2],
            in("x17") id,
            lateout("x10") ret
        );
    }
    ret
}

pub fn sys_write(fd: usize, buffer: &[u8]) -> isize {
    syscall(SYSCALL_WRITE, [fd, buffer.as_ptr() as usize, buffer.len()])
}

pub fn sys_exit(exit_code: i32) -> isize {
    syscall(SYSCALL_EXIT, [exit_code as usize, 0, 0])
}

pub fn sys_yield() -> isize {
    syscall(SYSCALL_YIELD, [0, 0, 0])
}
EOF

cat > src/lib.rs <<'EOF'
#![no_std]
#![feature(linkage)]

#[macro_use]
pub mod console;
mod syscall;
mod lang_items;

use syscall::*;

pub fn write(fd: usize, buf: &[u8]) -> isize {
    sys_write(fd, buf)
}

pub fn exit(exit_code: i32) -> isize {
    sys_exit(exit_code)
}

pub fn yield_() -> isize {
    sys_yield()
}

fn clear_bss() {
    extern "C" {
        fn start_bss();
        fn end_bss();
    }
    let start_bss_ptr = start_bss as *const () as usize;
    let end_bss_ptr = end_bss as *const () as usize;
    (start_bss_ptr..end_bss_ptr).for_each(|a| unsafe {
        (a as *mut u8).write_volatile(0)
    });
}

#[no_mangle]
#[link_section = ".text.entry"]
pub extern "C" fn _start() -> ! {
    clear_bss();
    exit(main());
    panic!("unreachable after sys_exit!");
}

#[linkage = "weak"]
#[no_mangle]
fn main() -> i32 {
    panic!("Cannot find main!");
}
EOF

cat > src/bin/00write_a.rs <<'EOF'
#![no_std]
#![no_main]

#[macro_use]
extern crate user_lib;

use user_lib::yield_;

const WIDTH: usize = 10;
const HEIGHT: usize = 5;

#[no_mangle]
fn main() -> i32 {
    for i in 0..HEIGHT {
        for _ in 0..WIDTH {
            print!("A");
        }
        println!(" [{}/{}]", i + 1, HEIGHT);
        yield_();
    }
    println!("Test write_a OK!");
    0
}
EOF

cat > src/bin/01write_b.rs <<'EOF'
#![no_std]
#![no_main]

#[macro_use]
extern crate user_lib;

use user_lib::yield_;

const WIDTH: usize = 10;
const HEIGHT: usize = 5;

#[no_mangle]
fn main() -> i32 {
    for i in 0..HEIGHT {
        for _ in 0..WIDTH {
            print!("B");
        }
        println!(" [{}/{}]", i + 1, HEIGHT);
        yield_();
    }
    println!("Test write_b OK!");
    0
}
EOF

cat > src/bin/02write_c.rs <<'EOF'
#![no_std]
#![no_main]

#[macro_use]
extern crate user_lib;

use user_lib::yield_;

const WIDTH: usize = 10;
const HEIGHT: usize = 5;

#[no_mangle]
fn main() -> i32 {
    for i in 0..HEIGHT {
        for _ in 0..WIDTH {
            print!("C");
        }
        println!(" [{}/{}]", i + 1, HEIGHT);
        yield_();
    }
    println!("Test write_c OK!");
    0
}
EOF

sed -n '1,120p' src/syscall.rs
sed -n '1,140p' src/lib.rs
ls src/bin
make build
```

成功标志：

- `src/syscall.rs` 中存在 `SYSCALL_YIELD = 124`。
- `src/lib.rs` 中存在 `yield_()`。
- `00write_a.rs`、`01write_b.rs`、`02write_c.rs` 能参与 `make build`。

截图占位：图4-4 用户态 yield 封装与测试应用构建。

REPORT_SYNC_BLOCK：

- 本阶段目标：让用户程序可以主动触发 `yield` 系统调用。
- 实际执行命令：见本阶段命令块。
- 成功标志：用户库和三个测试应用编译通过。
- 需要截图的位置：显示新增文件内容摘要和 `make build` 结果后截图整个命令行窗口。
- 本阶段修改的文件：`user/src/syscall.rs`、`user/src/lib.rs`、`user/src/bin/00write_a.rs`、`user/src/bin/01write_b.rs`、`user/src/bin/02write_c.rs`。
- 报告素材草稿：本阶段在用户态增加 `yield_` 封装，使应用能通过系统调用主动让出 CPU，为协作式调度提供触发点。
- 问题与处理：待填写。
- 是否建议进入下一 checkpoint：用户应用构建通过后进入阶段4。

## 阶段4：拆分内核配置和应用加载模块

目的：将实验3中批处理加载逻辑拆分为 `config.rs` 和 `loader.rs`，一次性加载多个应用到不同内存地址。

命令：

```bash
cd /mnt/os
cat > src/config.rs <<'EOF'
pub const USER_STACK_SIZE: usize = 4096 * 2;
pub const KERNEL_STACK_SIZE: usize = 4096 * 2;
pub const MAX_APP_NUM: usize = 16;
pub const APP_BASE_ADDRESS: usize = 0x80400000;
pub const APP_SIZE_LIMIT: usize = 0x20000;
EOF

cat > src/loader.rs <<'EOF'
use core::arch::asm;

use crate::config::*;
use crate::task::TaskContext;
use crate::trap::TrapContext;

#[repr(align(4096))]
#[derive(Copy, Clone)]
struct KernelStack {
    data: [u8; KERNEL_STACK_SIZE],
}

#[repr(align(4096))]
#[derive(Copy, Clone)]
struct UserStack {
    data: [u8; USER_STACK_SIZE],
}

static KERNEL_STACK: [KernelStack; MAX_APP_NUM] = [
    KernelStack { data: [0; KERNEL_STACK_SIZE] };
    MAX_APP_NUM
];

static USER_STACK: [UserStack; MAX_APP_NUM] = [
    UserStack { data: [0; USER_STACK_SIZE] };
    MAX_APP_NUM
];

impl KernelStack {
    fn get_sp(&self) -> usize {
        self.data.as_ptr() as usize + KERNEL_STACK_SIZE
    }

    pub fn push_context(&self, trap_cx: TrapContext, task_cx: TaskContext) -> &'static TaskContext {
        unsafe {
            let trap_cx_ptr = (self.get_sp() - core::mem::size_of::<TrapContext>()) as *mut TrapContext;
            *trap_cx_ptr = trap_cx;
            let task_cx_ptr = (trap_cx_ptr as usize - core::mem::size_of::<TaskContext>()) as *mut TaskContext;
            *task_cx_ptr = task_cx;
            task_cx_ptr.as_ref().unwrap()
        }
    }
}

impl UserStack {
    fn get_sp(&self) -> usize {
        self.data.as_ptr() as usize + USER_STACK_SIZE
    }
}

fn get_base_i(app_id: usize) -> usize {
    APP_BASE_ADDRESS + app_id * APP_SIZE_LIMIT
}

pub fn get_num_app() -> usize {
    extern "C" {
        fn _num_app();
    }
    unsafe { (_num_app as *const () as *const usize).read_volatile() }
}

pub fn load_apps() {
    extern "C" {
        fn _num_app();
    }
    let num_app_ptr = _num_app as *const () as *const usize;
    let num_app = get_num_app();
    let app_start = unsafe {
        core::slice::from_raw_parts(num_app_ptr.add(1), num_app + 1)
    };
    unsafe {
        asm!("fence.i");
    }
    for i in 0..num_app {
        let base_i = get_base_i(i);
        (base_i..base_i + APP_SIZE_LIMIT).for_each(|addr| unsafe {
            (addr as *mut u8).write_volatile(0)
        });
        let src = unsafe {
            core::slice::from_raw_parts(
                app_start[i] as *const u8,
                app_start[i + 1] - app_start[i],
            )
        };
        let dst = unsafe {
            core::slice::from_raw_parts_mut(base_i as *mut u8, src.len())
        };
        dst.copy_from_slice(src);
    }
}

pub fn init_app_cx(app_id: usize) -> &'static TaskContext {
    KERNEL_STACK[app_id].push_context(
        TrapContext::app_init_context(get_base_i(app_id), USER_STACK[app_id].get_sp()),
        TaskContext::goto_restore(),
    )
}
EOF

sed -n '1,120p' src/config.rs
sed -n '1,180p' src/loader.rs
```

成功标志：

- `config.rs` 中集中定义应用数量、栈大小和应用加载地址。
- `loader.rs` 中能读取 `_num_app`，并把所有应用复制到不同内存区间。
- `loader.rs` 中提供 `init_app_cx(app_id)`，为每个应用构造初始 Trap 上下文和任务上下文。

截图占位：图4-5 内核配置与应用加载模块。

REPORT_SYNC_BLOCK：

- 本阶段目标：建立多道程序加载的地址和加载模块。
- 实际执行命令：见本阶段命令块。
- 成功标志：`config.rs` 和 `loader.rs` 内容创建完成。
- 需要截图的位置：显示两个文件内容后截图整个命令行窗口。
- 本阶段修改的文件：`os/src/config.rs`、`os/src/loader.rs`。
- 报告素材草稿：本阶段将应用加载地址、应用大小限制和栈大小抽离为配置，并实现一次性加载多个应用的逻辑。
- 问题与处理：待填写。
- 是否建议进入下一 checkpoint：加载模块完成后进入阶段5。

## 阶段5：实现任务上下文、任务控制块和任务切换

目的：建立协作式调度所需的任务上下文保存、任务状态、任务控制块、任务管理器和汇编切换入口。

命令：

```bash
cd /mnt/os
mkdir -p src/task

cat > src/task/context.rs <<'EOF'
#[repr(C)]
pub struct TaskContext {
    ra: usize,
    s: [usize; 12],
}

impl TaskContext {
    pub fn goto_restore() -> Self {
        extern "C" {
            fn __restore();
        }
        Self {
            ra: __restore as *const () as usize,
            s: [0; 12],
        }
    }
}
EOF

cat > src/task/switch.S <<'EOF'
.altmacro
.macro SAVE_SN n
    sd s\n, (\n+1)*8(sp)
.endm
.macro LOAD_SN n
    ld s\n, (\n+1)*8(sp)
.endm
    .section .text
    .globl __switch
__switch:
    # __switch(
    #     current_task_cx_ptr2: &*const TaskContext,
    #     next_task_cx_ptr2: &*const TaskContext
    # )
    addi sp, sp, -13*8
    sd sp, 0(a0)
    sd ra, 0(sp)
    .set n, 0
    .rept 12
        SAVE_SN %n
        .set n, n + 1
    .endr
    ld sp, 0(a1)
    ld ra, 0(sp)
    .set n, 0
    .rept 12
        LOAD_SN %n
        .set n, n + 1
    .endr
    addi sp, sp, 13*8
    ret
EOF

cat > src/task/switch.rs <<'EOF'
use core::arch::global_asm;

global_asm!(include_str!("switch.S"));

extern "C" {
    pub fn __switch(
        current_task_cx_ptr2: *const usize,
        next_task_cx_ptr2: *const usize,
    );
}
EOF

cat > src/task/task.rs <<'EOF'
#[derive(Copy, Clone, PartialEq)]
pub enum TaskStatus {
    UnInit,
    Ready,
    Running,
    Exited,
}

#[derive(Copy, Clone)]
pub struct TaskControlBlock {
    pub task_cx_ptr: usize,
    pub task_status: TaskStatus,
}

impl TaskControlBlock {
    pub fn get_task_cx_ptr2(&self) -> *const usize {
        &self.task_cx_ptr as *const usize
    }
}
EOF

cat > src/task/mod.rs <<'EOF'
mod context;
mod switch;
mod task;

use core::cell::RefCell;

use lazy_static::*;

use crate::config::MAX_APP_NUM;
use crate::loader::{get_num_app, init_app_cx};

pub use context::TaskContext;
use switch::__switch;
use task::{TaskControlBlock, TaskStatus};

pub struct TaskManager {
    num_app: usize,
    inner: RefCell<TaskManagerInner>,
}

struct TaskManagerInner {
    tasks: [TaskControlBlock; MAX_APP_NUM],
    current_task: usize,
}

unsafe impl Sync for TaskManager {}

lazy_static! {
    pub static ref TASK_MANAGER: TaskManager = {
        let num_app = get_num_app();
        let mut tasks = [
            TaskControlBlock { task_cx_ptr: 0, task_status: TaskStatus::UnInit };
            MAX_APP_NUM
        ];
        for i in 0..num_app {
            tasks[i].task_cx_ptr = init_app_cx(i) as *const _ as usize;
            tasks[i].task_status = TaskStatus::Ready;
        }
        TaskManager {
            num_app,
            inner: RefCell::new(TaskManagerInner {
                tasks,
                current_task: 0,
            }),
        }
    };
}

impl TaskManager {
    fn run_first_task(&self) {
        self.inner.borrow_mut().tasks[0].task_status = TaskStatus::Running;
        let next_task_cx_ptr2 = self.inner.borrow().tasks[0].get_task_cx_ptr2();
        let _unused: usize = 0;
        unsafe {
            __switch(
                &_unused as *const _,
                next_task_cx_ptr2,
            );
        }
    }

    fn mark_current_suspended(&self) {
        let mut inner = self.inner.borrow_mut();
        let current = inner.current_task;
        inner.tasks[current].task_status = TaskStatus::Ready;
    }

    fn mark_current_exited(&self) {
        let mut inner = self.inner.borrow_mut();
        let current = inner.current_task;
        inner.tasks[current].task_status = TaskStatus::Exited;
    }

    fn find_next_task(&self) -> Option<usize> {
        let inner = self.inner.borrow();
        let current = inner.current_task;
        (current + 1..current + self.num_app + 1)
            .map(|id| id % self.num_app)
            .find(|id| {
                inner.tasks[*id].task_status == TaskStatus::Ready
            })
    }

    fn run_next_task(&self) {
        if let Some(next) = self.find_next_task() {
            let mut inner = self.inner.borrow_mut();
            let current = inner.current_task;
            inner.tasks[next].task_status = TaskStatus::Running;
            inner.current_task = next;
            let current_task_cx_ptr2 = inner.tasks[current].get_task_cx_ptr2();
            let next_task_cx_ptr2 = inner.tasks[next].get_task_cx_ptr2();
            core::mem::drop(inner);
            unsafe {
                __switch(
                    current_task_cx_ptr2,
                    next_task_cx_ptr2,
                );
            }
        } else {
            panic!("All applications completed!");
        }
    }
}

pub fn run_first_task() {
    TASK_MANAGER.run_first_task();
}

fn run_next_task() {
    TASK_MANAGER.run_next_task();
}

fn mark_current_suspended() {
    TASK_MANAGER.mark_current_suspended();
}

fn mark_current_exited() {
    TASK_MANAGER.mark_current_exited();
}

pub fn suspend_current_and_run_next() {
    mark_current_suspended();
    run_next_task();
}

pub fn exit_current_and_run_next() {
    mark_current_exited();
    run_next_task();
}
EOF

sed -n '1,120p' src/task/context.rs
sed -n '1,160p' src/task/switch.S
sed -n '1,220p' src/task/mod.rs
```

成功标志：

- `src/task` 下生成 `context.rs`、`switch.S`、`switch.rs`、`task.rs`、`mod.rs`。
- `TaskContext` 保存 `ra` 和 `s0-s11`。
- `TaskStatus` 包含 `Ready`、`Running`、`Exited`。
- `run_first_task`、`suspend_current_and_run_next`、`exit_current_and_run_next` 已建立。

截图占位：图4-6 任务上下文与任务管理结构。

REPORT_SYNC_BLOCK：

- 本阶段目标：实现协作式调度需要的任务抽象和上下文切换入口。
- 实际执行命令：见本阶段命令块。
- 成功标志：任务模块文件创建完成，核心结构和切换函数存在。
- 需要截图的位置：显示 `task` 目录和核心文件内容后截图整个命令行窗口。
- 本阶段修改的文件：`os/src/task/context.rs`、`os/src/task/switch.S`、`os/src/task/switch.rs`、`os/src/task/task.rs`、`os/src/task/mod.rs`。
- 报告素材草稿：本阶段为每个应用建立任务控制块，通过任务上下文保存内核态切换所需寄存器，并用 `__switch` 完成任务间切换。
- 问题与处理：待填写。
- 是否建议进入下一 checkpoint：任务模块完成后进入阶段6。

## 阶段6：接入 sys_yield 和调整 sys_exit

目的：让用户态 `yield_()` 能进入内核调度器，并让应用退出后切换到下一个任务。

命令：

```bash
cd /mnt/os
cat > src/syscall/mod.rs <<'EOF'
const SYSCALL_WRITE: usize = 64;
const SYSCALL_EXIT: usize = 93;
const SYSCALL_YIELD: usize = 124;

mod fs;
mod process;

use fs::*;
use process::*;

pub fn syscall(syscall_id: usize, args: [usize; 3]) -> isize {
    match syscall_id {
        SYSCALL_WRITE => sys_write(args[0], args[1] as *const u8, args[2]),
        SYSCALL_EXIT => sys_exit(args[0] as i32),
        SYSCALL_YIELD => sys_yield(),
        _ => panic!("Unsupported syscall_id: {}", syscall_id),
    }
}
EOF

cat > src/syscall/process.rs <<'EOF'
use crate::task::{exit_current_and_run_next, suspend_current_and_run_next};

pub fn sys_exit(exit_code: i32) -> ! {
    println!("[kernel] Application exited with code {}", exit_code);
    exit_current_and_run_next();
    panic!("Unreachable in sys_exit!");
}

pub fn sys_yield() -> isize {
    suspend_current_and_run_next();
    0
}
EOF

sed -n '1,120p' src/syscall/mod.rs
sed -n '1,120p' src/syscall/process.rs
```

成功标志：

- `SYSCALL_YIELD` 已加入系统调用分发表。
- `sys_yield()` 调用 `suspend_current_and_run_next()`。
- `sys_exit()` 调用 `exit_current_and_run_next()`。

截图占位：图4-7 sys_yield 与 sys_exit 调度接入。

REPORT_SYNC_BLOCK：

- 本阶段目标：把系统调用和任务调度连接起来。
- 实际执行命令：见本阶段命令块。
- 成功标志：系统调用分发表和 process 系统调用实现已更新。
- 需要截图的位置：显示两个文件内容后截图整个命令行窗口。
- 本阶段修改的文件：`os/src/syscall/mod.rs`、`os/src/syscall/process.rs`。
- 报告素材草稿：本阶段将 `yield` 系统调用映射到任务挂起和切换逻辑，将 `exit` 系统调用映射到任务退出和继续调度逻辑。
- 问题与处理：待填写。
- 是否建议进入下一 checkpoint：系统调用接入后进入阶段7。

## 阶段7：修改 Trap 上下文、入口代码并构建运行

目的：让内核初始化加载全部应用、初始化任务管理器，并从第一个任务开始运行。

命令：

```bash
cd /mnt/os
cat > src/trap/trap.S <<'EOF'
.altmacro

.macro SAVE_GP n
    sd x\n, \n*8(sp)
.endm

.macro LOAD_GP n
    ld x\n, \n*8(sp)
.endm

    .section .text
    .globl __alltraps
    .globl __restore
    .align 2

__alltraps:
    csrrw sp, sscratch, sp
    addi sp, sp, -34*8
    sd x1, 1*8(sp)
    sd x3, 3*8(sp)
    .set n, 5
    .rept 27
        SAVE_GP %n
        .set n, n+1
    .endr
    csrr t0, sstatus
    csrr t1, sepc
    sd t0, 32*8(sp)
    sd t1, 33*8(sp)
    csrr t2, sscratch
    sd t2, 2*8(sp)
    mv a0, sp
    call trap_handler

__restore:
    ld t0, 32*8(sp)
    ld t1, 33*8(sp)
    ld t2, 2*8(sp)
    csrw sstatus, t0
    csrw sepc, t1
    csrw sscratch, t2
    ld x1, 1*8(sp)
    ld x3, 3*8(sp)
    .set n, 5
    .rept 27
        LOAD_GP %n
        .set n, n+1
    .endr
    addi sp, sp, 34*8
    csrrw sp, sscratch, sp
    sret
EOF

cat > src/trap/context.rs <<'EOF'
use core::arch::asm;

#[repr(C)]
pub struct TrapContext {
    pub x: [usize; 32],
    pub sstatus: usize,
    pub sepc: usize,
}

impl TrapContext {
    pub fn set_sp(&mut self, sp: usize) {
        self.x[2] = sp;
    }

    pub fn app_init_context(entry: usize, sp: usize) -> Self {
        let mut sstatus = read_sstatus();
        sstatus &= !(1 << 8);
        let mut cx = Self {
            x: [0; 32],
            sstatus,
            sepc: entry,
        };
        cx.set_sp(sp);
        cx
    }
}

fn read_sstatus() -> usize {
    let bits: usize;
    unsafe {
        asm!("csrr {}, sstatus", out(reg) bits);
    }
    bits
}
EOF

cat > src/trap/mod.rs <<'EOF'
use core::arch::{asm, global_asm};

global_asm!(include_str!("trap.S"));

mod context;

use crate::syscall::syscall;

pub use context::TrapContext;

pub fn init() {
    extern "C" {
        fn __alltraps();
    }
    unsafe {
        asm!("csrw stvec, {}", in(reg) __alltraps as usize);
    }
}

#[no_mangle]
pub fn trap_handler(cx: &mut TrapContext) -> &mut TrapContext {
    let scause = read_scause();
    let stval = read_stval();
    let is_interrupt = (scause >> 63) != 0;
    let cause = scause & 0xfff;

    if !is_interrupt {
        match cause {
            8 => {
                cx.sepc += 4;
                cx.x[10] = syscall(cx.x[17], [cx.x[10], cx.x[11], cx.x[12]]) as usize;
            }
            7 | 15 => {
                panic!("[kernel] PageFault in application, stval = {:#x}", stval);
            }
            2 => {
                panic!("[kernel] IllegalInstruction in application, stval = {:#x}", stval);
            }
            _ => {
                panic!("Unsupported trap {}, stval = {:#x}!", scause, stval);
            }
        }
    } else {
        panic!("Unsupported interrupt {}, stval = {:#x}!", scause, stval);
    }
    cx
}

fn read_scause() -> usize {
    let bits: usize;
    unsafe {
        asm!("csrr {}, scause", out(reg) bits);
    }
    bits
}

fn read_stval() -> usize {
    let bits: usize;
    unsafe {
        asm!("csrr {}, stval", out(reg) bits);
    }
    bits
}
EOF

cat > src/main.rs <<'EOF'
#![no_std]
#![no_main]
#![feature(panic_info_message)]

#[macro_use]
mod console;
mod config;
mod lang_items;
mod loader;
mod sbi;
mod syscall;
mod task;
mod trap;

use core::arch::global_asm;

global_asm!(include_str!("entry.asm"));
global_asm!(include_str!("link_app.S"));

fn clear_bss() {
    extern "C" {
        fn sbss();
        fn ebss();
    }
    (sbss as usize..ebss as usize).for_each(|a| unsafe {
        (a as *mut u8).write_volatile(0);
    });
}

#[no_mangle]
pub fn rust_main() -> ! {
    clear_bss();
    println!("[Kernel] Hello, world!");
    trap::init();
    loader::load_apps();
    task::run_first_task();
    panic!("Unreachable in rust_main!");
}
EOF

sed -n '1,160p' src/trap/trap.S
sed -n '1,180p' src/trap/mod.rs
sed -n '1,120p' src/main.rs

cd /mnt/user
cat > build.py <<'EOF'
import os
import shutil
from pathlib import Path

base_address = 0x80400000
step = 0x20000
linker = 'src/linker.ld'
target = 'riscv64gc-unknown-none-elf'
mode = 'release'
objcopy = 'rust-objcopy --binary-architecture=riscv64'

linker_path = Path(linker)
target_dir = Path('target') / target / mode
save_dir = Path('.build-app-cache')
EOF

cat >> build.py <<'EOF'
if save_dir.exists():
    shutil.rmtree(save_dir)
save_dir.mkdir(parents=True)

apps = sorted(path.stem for path in Path('src/bin').glob('*.rs'))
original_linker = linker_path.read_text()

try:
    for app_id, app in enumerate(apps):
        app_base = base_address + step * app_id
        linker_path.write_text(original_linker.replace(hex(base_address), hex(app_base)))

        os.system('cargo clean')
        ret = os.system('cargo build --bin %s --release' % app)
        if ret != 0:
            raise SystemExit(ret)

        elf = target_dir / app
        bin_file = target_dir / (app + '.bin')
        ret = os.system('%s %s --strip-all -O binary %s' % (objcopy, elf, bin_file))
        if ret != 0:
            raise SystemExit(ret)

        shutil.copy2(elf, save_dir / app)
        shutil.copy2(bin_file, save_dir / (app + '.bin'))
        print('[build.py] application %s start with address %s' % (app, hex(app_base)))
finally:
    linker_path.write_text(original_linker)

target_dir.mkdir(parents=True, exist_ok=True)
for saved in save_dir.iterdir():
    shutil.copy2(saved, target_dir / saved.name)
shutil.rmtree(save_dir)
EOF

sed -n '1,160p' build.py

mkdir -p src/bin-exp3-backup
mv src/bin/00hello_world.rs src/bin-exp3-backup/
mv src/bin/01store_fault.rs src/bin-exp3-backup/
mv src/bin/02power.rs src/bin-exp3-backup/
ls src/bin
ls src/bin-exp3-backup
make build

rust-readobj -h target/riscv64gc-unknown-none-elf/release/00write_a | grep Entry
rust-readobj -h target/riscv64gc-unknown-none-elf/release/01write_b | grep Entry
rust-readobj -h target/riscv64gc-unknown-none-elf/release/02write_c | grep Entry

cd /mnt/os
cargo clean
cargo build --release
sed -n '1,120p' src/link_app.S
make run
```

成功标志：

- `make build` 只构建 `00write_a`、`01write_b`、`02write_c` 三个实验4应用。
- 三个用户程序入口地址分别为 `0x80400000`、`0x80420000`、`0x80440000`。
- `src/link_app.S` 中 `_num_app` 为 `3`，并按 A/B/C 顺序嵌入三个 `.bin` 文件。
- `make run` 启动 OpenSBI 和内核，输出中能看到 A/B/C 通过 `yield_()` 交替运行。
- 应用执行结束后出现 `Test write_a OK!`、`Test write_b OK!`、`Test write_c OK!`，最后 `All applications completed!` 可作为所有任务完成的结束现象。

截图占位：图4-8 实验4多道程序协作式调度运行结果。

REPORT_SYNC_BLOCK：

- 本阶段目标：完成实验4主流程接入并运行验证。
- 实际执行命令：见本阶段命令块。
- 成功标志：内核运行后多个用户程序能够通过 `yield` 交替输出。
- 需要截图的位置：显示构建成功和 QEMU 运行结果后截图整个命令行窗口。
- 本阶段修改的文件：`os/src/trap/trap.S`、`os/src/trap/context.rs`、`os/src/trap/mod.rs`、`os/src/main.rs`、`user/build.py`，并将实验3旧应用临时移入 `user/src/bin-exp3-backup`。
- 报告素材草稿：本阶段在内核启动时加载三个实验4应用并运行第一个任务。用户程序通过 `yield` 主动让出 CPU，调度器保存当前任务上下文并切换到下一个就绪任务，最终 A/B/C 三个应用交替输出并正常退出。
- 问题与处理：初次运行时旧实验3应用混入，且 Cargo 未因临时修改 `linker.ld` 而重新链接用户程序，导致入口地址与加载地址不一致。处理方式是临时移出旧应用，并在 `user/build.py` 中对每个应用执行 `cargo clean` 后重新构建，确认三个应用入口地址分别为 `0x80400000`、`0x80420000`、`0x80440000`。
- 是否建议进入下一 checkpoint：运行结果符合预期后进入阶段8。

## 阶段8：Git 证据与报告整理

目的：只查看当前工作区变化，为后续提交和报告截图做准备。正式 `git add`、`git commit`、`git push` 需要单独授权。

命令：

```bash
cd /mnt
git status --short
git diff --stat
```

成功标志：

- 能看到实验4新增或修改的文件列表。
- 不执行暂存、提交或推送。

截图占位：图4-9 实验4 Git 工作区状态。

REPORT_SYNC_BLOCK：

- 本阶段目标：整理实验4文件变化和提交前证据。
- 实际执行命令：见本阶段命令块。
- 成功标志：能查看工作区状态和差异统计。
- 需要截图的位置：显示 `git status --short` 和 `git diff --stat` 后截图整个命令行窗口。
- 本阶段修改的文件：无新增修改，仅查看状态。
- 报告素材草稿：本阶段查看实验4涉及的用户态、内核态和报告文件变更，为后续提交截图做准备。
- 问题与处理：待填写。
- 是否建议进入下一 checkpoint：若需要提交，请先明确授权 Git 暂存、提交和推送。

## 思考问题占位

### 思考问题1：分析应用程序是如何加载的

待根据真实代码和运行结果补写。建议围绕 `user/build.py` 的不同链接地址、`link_app.S` 中的应用二进制嵌入、`loader::load_apps()` 的内存复制过程说明。

### 思考问题2：分析多道程序如何设计和实现的

待根据真实代码和运行结果补写。建议围绕应用分区加载、任务控制块、任务状态、就绪任务查找和协作式 `yield` 调度说明。

### 思考问题3：任务与理论课程中进程、线程的区别和联系

待根据真实代码和运行结果补写。建议说明本实验任务是教学内核中的执行单元，保存了上下文和运行状态，但还没有完整进程地址空间隔离，也没有线程共享进程资源的完整抽象。
