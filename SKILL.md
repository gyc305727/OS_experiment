# SKILL.md

## 名称

操作系统实验报告生成与逐阶段验证

## 适用场景

当用户在本仓库中要求准备、生成、修正或验证操作系统实验报告时，使用本技能。尤其适用于实验6“内存管理”：先生成带截图占位符的完整报告草稿，再按阶段让用户执行命令、贴回输出、截图并同步修正报告。

## 输入资料

优先读取：

1. `AGENTS.md`
2. `MEMORY.md`
3. 当前实验说明，例如 `docs/exp6-memory-management/实验6：内存管理.txt`
4. 已有实验报告目录，例如 `reports/`
5. 当前代码目录，例如 `os/`、`user/`

不要依赖旧的 `records/*/*过程记录.md`。本项目不再维护单独过程记录文件。

## 工作流

1. 明确实验编号、实验名称和报告目标。
2. 阅读实验要求和已有报告风格，拆分可独立验证的阶段。
3. 生成报告草稿，放入对应 `reports/exp*/` 目录。
4. 在报告中预置截图占位符、逐行命令、命令解释、成功标志和待填真实输出位置。
5. 每次只给用户一个阶段的命令。
6. 用户执行命令并贴回真实输出。
7. 根据输出判断是否通过。
8. 通过后提醒截图整个命令行窗口，并把截图名对应到报告占位符。
9. 出错时先给恢复到上一阶段结束状态的命令，再给新的当前阶段方案。
10. 新方案验证成功后，只更新报告中当前阶段相关内容。
11. 全部阶段完成后，核对报告、截图、命令和真实输出是否一致。

## 报告阶段模板

每个阶段建议包含：

```markdown
### 阶段 N：阶段名称

**目的：**
说明本阶段要验证或实现什么。

**执行环境：**
Windows PowerShell 仓库根目录 / 容器 `/mnt` / 容器 `/mnt/os` / 容器 `/mnt/user`。

**逐行命令：**
```powershell
命令 1
命令 2
```

**命令说明：**
逐条说明命令本体、参数、路径、管道或重定向的含义。

**成功标志：**
说明应出现的关键输出或现象。

**真实输出：**
待用户执行后粘贴。

**截图占位：**
`实验N-第M阶段.1-内容说明.png`

**修改文件：**
列出本阶段新增或修改的文件。

**问题与处理：**
未验证前写“待执行后填写”；若出错，记录真实问题和恢复方案。
```

## 命令设计规则

- 命令必须能逐行复制执行。
- 每组命令必须从明确目录开始。
- 容器内命令必须显式 `cd /mnt`、`cd /mnt/os` 或 `cd /mnt/user`。
- 避免超长 heredoc；长文件优先分模块、分阶段或由 Codex 直接修改工作区文件。
- 不把预期输出写成真实输出。
- 不跳过构建和运行验证。

## 失败处理规则

失败后不要继续叠加修改。先恢复到上一阶段成功状态，再重新给当前阶段命令。

恢复方案必须说明：

- 恢复目标。
- 会覆盖、删除或还原哪些当前阶段内容。
- 哪些已完成阶段不受影响。
- 为什么新方案比原方案更适合当前真实输出。

## 截图命名

默认格式：

`实验N-第M阶段.K-内容说明.png`

截图必须对应报告占位符。若用户实际文件名不同，及时更新报告。
